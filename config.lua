Config = {}

-- The duration of the hitman mission in seconds
Config.MissionDuration = 300

-- The radius within which the player needs to be to start the mission
Config.StartMissionDistance = 5.0

-- The reward amount for completing the hitman mission
Config.RewardAmount = 5000

-- The list of gang IDs that hitmen can belong to
Config.GangList = {
    "GANG_1",
    "GANG_2",
    "GANG_3"
}